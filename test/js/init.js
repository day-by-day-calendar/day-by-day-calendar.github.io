function setLanguage(lng,page) {          if (page == undefined) page = "index.html";            if (lng == "ru" || lng == "rus") {               window.location.href = "ru/" + page;           }            if (lng == "en" || lng == "eng") {                window.location.href = "../" + page;           }                document.cookie = "lang = " +lng +";path = /";    }function setVisibleByWidth() {    if (document.body.clientWidth < 400) document.getElementById("col_right").style.display = "none";    if (document.body.clientWidth < 400) document.getElementById("img_logo").style.display = "none";        setLanguageInit();}function setLanguageInit() {    if (getCookie("lang") == undefined ) {        setLanguage(navigator.language || navigator.userLanguage,getNameCurrPage());    }}function getNameCurrPage() {        var st = unescape(window.location.href );        var r = st.substring(st.lastIndexOf('/') + 1, st.length );        return r}function getCookie(cookie_name) {  var results = document.cookie.match ( '(^|;) ?' + cookie_name + '=([^;]*)(;|$)' );  if ( results )    return ( unescape ( results[2] ) );  else    return null;}